# StoryBridge 录屏讲解稿

> 建议时长：4 分 30 秒～5 分钟  
> 录制形式：屏幕操作 + 中文语音讲解  
> 推荐模式：真实模型模式；Mock 仅用于录制前排练  
> 核心内容：设计思路、使用流程、产品特点、工程可靠性

## 一、录制前准备

### 1. 启动项目

在仓库根目录运行：

```bash
./speed_run.sh
```

看到以下提示后打开浏览器：

```text
StoryBridge is ready (real mode).
App:      http://127.0.0.1:5173
API docs: http://127.0.0.1:8000/docs
```

注意：录屏时不要打开 `backend/.env`，也不要展示 API Key、余额或终端环境变量。

### 2. 准备演示项目

建议提前完整运行一次真实项目，保证历史记录中至少有一个已经完成下列流程的项目：

```text
Analyze → Plan → Propagate → Apply → Verify → Render
```

现场生成时间过长时，可以剪掉等待画面，或者从项目侧栏打开提前完成的项目继续讲解。

### 3. 推荐演示剧本

```text
S01：相亲饭局上，苏母质疑林晓东没有稳定编制。
S02：苏父认为没有编制就无法给女儿未来。
S03：女方提出三十八万八彩礼。
S04：林晓东为筹彩礼放弃晋升机会。
S05：苏婉误会林晓东没有上进心，提出分手。
S06：三年后林晓东创业成功，苏婉被安排相亲。
S07：同学会上，985 毕业的赵子昂当众羞辱林晓东。
S08：会议上揭晓林晓东才是公司创始人。
```

目标市场建议填写：

| 字段 | 内容 |
|---|---|
| 市场 | United States |
| 受众 | 18–30 |
| 形式 | Short drama |
| 类型 | Urban drama |
| 目标语言 | English |
| 目标 locale | en-US |

## 二、时间安排

| 时间 | 展示内容 |
|---|---|
| 0:00–0:25 | 启动与项目定位 |
| 0:25–1:05 | 设计思路与 Agent Loop |
| 1:05–1:45 | 新建项目、输入剧本与分析 |
| 1:45–2:25 | Story State 与文化摩擦结果 |
| 2:25–3:15 | 多文化点选择与 A/B/C 方案 |
| 3:15–4:05 | 依赖传播、批量改写和验证 |
| 4:05–4:35 | 版本、历史记录和英文剧本 |
| 4:35–5:00 | 特点总结 |

## 三、完整逐字稿

### 0:00–0:25　启动与项目定位

**画面操作**

1. 在终端运行 `./speed_run.sh`。
2. 出现 ready 提示后切换到 StoryBridge 首页。
3. 鼠标停留在首页标题处。

**讲解词**

> 大家好，我们的项目叫 StoryBridge，是一个面向中文短剧和网文出海的跨文化故事改编智能体。
>
> 项目可以通过一条命令启动。Python、Node 和前端依赖在首次安装后会保存在项目本地，后续启动会直接复用。
>
> StoryBridge 要解决的并不是一句话怎样翻译，而是一个中国文化设定进入海外市场以后，人物动机、剧情因果和情绪效果是否仍然成立。

### 0:25–1:05　设计思路与 Agent Loop

**画面操作**

1. 缓慢滚动首页。
2. 展示首页的 Story、State、Adaptation 和 Verification 文案。

**讲解词**

> 例如，“编制”不仅是一个职业名词，它还可能代表经济安全、社会地位和婚姻市场中的认可；“彩礼”也不只是一个金额，它可能推动借钱、误会、分手和最后的身份反转。
>
> 如果只把这些词翻译成英文，海外观众可能看懂字面，却无法理解人物为什么这样行动。
>
> 因此，StoryBridge 首先把剧本解析成显式的 Story State，其中包括人物、场景、事件、文化机制和叙事承诺；然后构建 Dependency Graph，记录这些元素之间的依赖关系。
>
> 当用户改变一个文化点时，智能体会沿依赖图寻找所有受到影响的场景，完成改写后再统一验证。整个流程是 Observe、Plan、Act、Verify 和 Revise，而不是一次不可解释的模型调用。

### 1:05–1:45　新建项目、输入剧本与分析

**画面操作**

1. 点击右上角“项目”。
2. 展示“新建项目”和“历史记录”。
3. 点击“新建项目”。
4. 粘贴准备好的剧本。
5. 展示目标市场画像。
6. 点击“创建项目并分析”。

**讲解词**

> 右上角的项目侧栏可以创建新项目，也可以恢复历史记录。刷新页面后，系统会继续恢复当前任务；点击“新建项目”则会回到空白工作区，但不会删除以前的项目。
>
> 这里输入一份以婚姻冲突和身份反转为主线的中文短剧，并设置目标市场、受众、内容形式、类型、目标语言和 locale。这次我们选择美国市场，目标语言为 English。
>
> 点击分析后，前端会提交一个异步 Analyze Job。对于超长文本，后端会按照稳定场景边界分块处理，并合并跨块人物、场景和叙事承诺。任务和中间状态都会持久化，所以刷新页面不会重复调用模型。

> 【如果等待超过五秒，从这里剪辑到分析完成后的画面。】

### 1:45–2:25　Story State 与文化摩擦结果

**画面操作**

1. 展示场景、角色、文化机制和依赖关系统计。
2. 缓慢滚动“编制”“彩礼”“985”等文化机制卡片。
3. 将鼠标停留在原文证据和叙事功能区域。

**讲解词**

> 分析完成后，页面会展示 Story State 的摘要，包括场景、角色、文化机制和依赖关系。
>
> 在这份剧本中，系统识别出了“编制”“彩礼”和“985”等文化机制。每张卡片都会显示原文证据、出现位置、文化摩擦程度和叙事重要度。
>
> 系统还会标注这个机制承担的剧情、社会和情绪功能。例如，“编制”可能同时承担社会地位、经济安全和羞辱感；“彩礼”可能承担家庭义务、经济压力和分手冲突。
>
> 后续改编真正需要保留的不是原来的词，而是这些词在故事中承担的功能。

> 【这里应按照页面实际结果讲数字，不要在数据不一致时固定说“八个场景”或“三个机制”。】

### 2:25–3:15　多文化点选择与 A/B/C 方案

**画面操作**

1. 保留默认选中的第一个文化点。
2. 再点击“彩礼”或另一个文化点。
3. 确认页面显示“2 个文化点”。
4. 点击“为 2 个点生成方案”。
5. 展示每个文化点的 A/B/C 卡片。
6. 第一个文化点选择 B，第二个文化点选择 C。

**讲解词**

> StoryBridge 支持一次选择多个文化点。这里我同时选择两个机制，让智能体在同一份候选剧本上按顺序反复迭代，避免第二次改写覆盖第一次结果。
>
> 对每个文化点，系统都会生成三种不同强度的方案。
>
> 方案 A 是保留并解释，适合保留原有文化特色；方案 B 是功能性替换，也就是在目标市场中寻找承担相同剧情、社会和情绪功能的具体设定；方案 C 是情节重构，适合原机制无法直接迁移、需要重新设计冲突来源的情况。
>
> 例如，“编制”可以功能性替换为传统大机构中的稳定职位和明确晋升通道；如果选择情节重构，也可以把冲突改为债务、职业风险或家庭经济责任。
>
> 方案标题、替换定义、理由和风险使用中文展示，方便中文创作者决策；必要的目标文化专有名词仍然可以保留英文。

### 3:15–4:05　依赖传播、批量改写和验证

**画面操作**

1. 展示各文化点的影响场景。
2. 展示 Story Graph 高亮节点和关系说明。
3. 点击“批量改编 2 个文化点”。
4. 生成完成后展示改写场景、Before/After Diff 和一致性验证。

**讲解词**

> 选择方案后，系统不会立刻改写，而是先进行 Dependency Propagation，也就是依赖传播。
>
> 页面会展示每个文化点影响了哪些场景、为什么受到影响，以及传播路径经过了哪些事件或叙事承诺。依赖图中高亮的部分就是本次批量改编涉及的节点。
>
> 确认后，智能体会按照选择顺序逐点 Rewrite。第一个文化点完成后，第二个文化点读取的是已经改写过的候选剧本。所有文化点处理完成后，系统才统一验证并原子提交；中途失败不会留下只修改一半的状态。
>
> 改写完成后，页面会展示修改后的场景和相对于初始版本的 Diff。Verifier 会检查旧文化词是否残留、关键场景是否漏改，以及人物动机、因果关系和叙事承诺是否仍然成立。
>
> 如果发现可修复问题，系统会进行有限次数的自动修复；如果仍然不能确定，会明确标记为 needs review，而不是把结果伪装成完全通过。

### 4:05–4:35　版本、历史记录和英文剧本

**画面操作**

1. 展示 Revision History。
2. 展示“完整演示产物”。
3. 点击“生成 English 剧本”。
4. 展示英文完整剧本、版本号和 `en-US`。
5. 打开项目侧栏，快速展示历史记录。

**讲解词**

> 每次成功改编都会生成新的 Story State 版本。例如，初始分析结果是 v1，批量改编完成后成为 v2。
>
> 改编方案会绑定生成时的版本，因此旧方案不能错误地应用到已经变化的新剧本上。项目、任务、方案、修订记录和目标语言产物统一保存在 SQLite 中。
>
> 最后，系统根据已经冻结的结构改编稿生成完整英文剧本。目标语言产物会记录 source state version，保证当前英文稿明确来源于 v2，而不是某个已经过期的中间版本。
>
> 前面的分析、方案选择和验证保持中文，只有最终交付稿使用目标语言，从而把创作决策和语言生成清晰分开。

### 4:35–5:00　总结

**画面操作**

1. 回到依赖图、Diff 或最终剧本中视觉效果最好的一处。
2. 停止操作，让画面保持稳定。

**讲解词**

> StoryBridge 的特点可以概括为三点。
>
> 第一，它处理的是文化机制和叙事功能，而不只是语言翻译。
>
> 第二，它通过 Story State 和 Dependency Graph 追踪一次设定变化的下游影响。
>
> 第三，它允许创作者同时选择多个文化点和不同改编策略，并通过版本、Diff 和验证器，让结果可以追踪、检查和恢复。
>
> StoryBridge 不替代创作者做最终的文化判断，而是把跨文化改编从一次黑盒生成，变成一个由创作者参与决策、由智能体负责分析和执行的可靠工作流。以上就是我们的项目展示，谢谢大家。

## 四、超时后的删减顺序

如果录制超过 5 分钟，按照以下顺序删减：

1. 删除超长文本分块的详细解释，只保留“长文本支持分块与恢复”。
2. 删除“编制”的方案举例。
3. 将 SQLite 部分缩短为“项目和版本会被持久化”。
4. 将 A/B/C 方案压缩为每种一句话。

不要删掉以下核心画面：

1. 输入剧本和目标市场。
2. 文化机制与叙事功能。
3. 同时选择两个文化点。
4. 为不同文化点选择不同方案。
5. 依赖传播与 Story Graph。
6. 批量改写、Diff 和验证。
7. 最终英文剧本和版本号。

## 五、录屏检查清单

- [ ] 使用真实模型模式，Mock 只用于排练。
- [ ] 历史记录中准备一个完整项目作为备用。
- [ ] 浏览器缩放设置为 80%～90%。
- [ ] 关闭无关通知、聊天窗口和浏览器标签页。
- [ ] 不展示 `.env`、API Key、模型余额或环境变量。
- [ ] 模型等待超过五秒时剪辑或加速。
- [ ] 鼠标点击前停留半秒，避免操作过快。
- [ ] 页面实际数字与讲解词保持一致。
- [ ] 不使用“完全保证质量”等过度承诺。
- [ ] 最终成片控制在 3～5 分钟。
- [ ] 导出后完整观看一遍，确认声音清晰、文字可读。

## 六、表达注意事项

建议使用以下表达：

- “降低跨场景漏改风险。”
- “辅助创作者完成文化决策。”
- “结果可以追踪、验证和恢复。”
- “未确定的问题会进入人工复核。”

避免使用以下表达：

- “系统可以完全替代编剧或翻译人员。”
- “系统能够保证所有改编结果正确。”
- “一致性分数等同于真实改编质量。”
- “Mock 结果可以证明真实模型质量。”
